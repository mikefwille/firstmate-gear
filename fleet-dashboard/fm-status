#!/usr/bin/env bash
# Thin launcher for the fleet board. Runs the single-file uv script, which
# self-bootstraps its deps (Rich) on first run. Just: ./fm-status [--watch]
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec uv run --quiet "$here/fm_status.py" "$@"
